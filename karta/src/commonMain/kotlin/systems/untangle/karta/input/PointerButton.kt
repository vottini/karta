package systems.untangle.karta.input

/** The pointer button involved in a [ButtonEvent]. */
enum class PointerButton {
    /** Primary (left) mouse button or a single-finger tap. */
    LEFT,
    /** Secondary (right) mouse button. */
    RIGHT,
    /** Mouse scroll wheel button (middle click). */
    WHEEL
}
